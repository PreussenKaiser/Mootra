@using Mootra.Core.Models

@code
{
    /// <summary>
    /// Gets or sets items in the carousel.
    /// </summary>
    [Parameter]
    public IEnumerable<Emotion> Items { get; set; }

    /// <summary>
    /// Gets or initializes the action to execute when an item is selected.
    /// </summary>
    [Parameter]
    public Action<Emotion> OnSelect { get; init; }
}
